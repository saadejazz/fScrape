# The maximum number of concurrent requests to send per website
MAX_CONCURRENT_REQUESTS = 10

# Location to dump images temporarily
DUMP = "dump"

# Row height pixel multiplier
R_MULTI = 1.2

# Column width pixel multiplier
C_MULTI = 7.0

# Folder to store resulting excel files
OUT_DIRECTORY = "results"

# Max number of pages to search for to get listings
MAX_PAGES_LISTING = 150

# The number of attempts to connect before giving up
RETRY_ATTEMPTS = 3

# delay between subsequent retry requests
RETRY_DELAY = 0.5

# the default category to scrape
DEFAULT = "Furniture"